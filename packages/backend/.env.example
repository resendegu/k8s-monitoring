# Session secret for Express session management (required)
# Generate a secure random string for production
SESSION_SECRET=your-secret-key-here

# Server port (optional, defaults to 3001)
PORT=3001

# CORS Configuration (required for public/Cloudflare deployments)
# Comma-separated list of allowed origins
# Examples:
#   - Specific domain: https://yourdomain.com
#   - Wildcard subdomain: https://*.yourdomain.com
#   - Multiple: https://yourdomain.com,https://app.yourdomain.com
#   - Allow all (NOT recommended): *
ALLOWED_ORIGINS=https://yourdomain.com

# Cookie configuration for HTTPS behind Cloudflare
# Set to true if your frontend is accessed via HTTPS (even if backend is HTTP behind proxy)
SECURE_COOKIES=true
