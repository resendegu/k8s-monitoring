# Environment Configuration
NODE_ENV=production

# Server Configuration
PORT=3001
API_BASE_URL=/api

# Session Secret (REQUIRED - Generate a secure random string)
SESSION_SECRET=your-super-secret-session-key-here-change-this-in-production

# CORS Configuration (REQUIRED for Cloudflare/public deployments)
# Comma-separated list of allowed origins
# Examples:
#   - Specific domain: https://yourdomain.com
#   - Wildcard subdomain: https://*.yourdomain.com
#   - Multiple: https://yourdomain.com,https://app.yourdomain.com
#   - Allow all (NOT recommended for production): *
ALLOWED_ORIGINS=https://yourdomain.com

# Cookie Configuration (REQUIRED when behind Cloudflare/HTTPS proxy)
# Set to true if your frontend is accessed via HTTPS
# even if the backend container runs on HTTP behind a proxy
SECURE_COOKIES=true

# AI Provider API Keys (optional - can be configured via UI)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=AIzaSy...

# Frontend Configuration (for production builds)
VITE_API_URL=

# Kubernetes Configuration (optional)
# KUBECONFIG_PATH=/app/config/kubeconfig.yaml
# IN_CLUSTER_MODE=false
